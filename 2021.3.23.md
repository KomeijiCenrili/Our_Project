2021.3.23

项目的进度不是很理想，学长说4月20号左右就得把作品做出来。还有不到一个月，课程也挺紧的，略恐怖，能开心的活着这件事本身，要比挑战杯更有挑战。

现在的情况是前端勉强能用，智能合约还没写，预言机任务已经选好，接口正在开发，同态加密说是用开源的那个Paillier，但这一块我一无所知，得抽空问问sakii宝。

今天有了突破性的进展（对自己而言，成功把express框架部署到了阿里云上。访问对应的地址，可以返回字符串数据了。

但接口的开发，涉及的功能挺多的：post上传数据到后端、get从后端获取数据、数据的类型。现在只是做到了get从后端获取数据，而post和数据的类型还没解决。这一部分的问题还得继续学web开发，这周之内，一定得做出来。

post上传数据，同样挺麻烦，涉及上位机的开发，即把单片机的数据通过串口获取到电脑上。上位机用C#开发，那么C#获取的数据怎么上传到后端，同样是个问题。

总结一下对于项目开发，我还需要做的事情吧：

1. 实现后端传送json格式的数据
2. 实现C#编写上位机
3. C#获取的数据上传到后端
4. post上传数据到后端（和3有对应
5. 负责solidity的同学进度不太理想，我抽出时间来自己试试看
6. 同态加密的事，得赶紧问问sakii
7. 周末的答辩，今天问学长改了ppt，得赶紧准备